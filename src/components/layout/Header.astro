---
import { Image } from 'astro:assets';
import logo from '../../assets/logo/logo_hidromont.png';
import { navItems, ctaLabel, ctaHref } from '../../data/nav';
import Button from '../ui/Button.astro';
import EditableText from '../cms/EditableText.astro';
import EditableImage from '../cms/EditableImage.astro';
import { getCmsText } from '../../data/cms';

const cmsLogoSrc = getCmsText('layout.header', 'logoSrc', '');
const cmsLogoAlt = getCmsText('layout.header', 'logoAlt', 'Hidromont Chile S.A.');

interface Props {
  currentPath?: string;
  overlay?: boolean;
}

const { currentPath = '/', overlay = false } = Astro.props;

const mobileMenuOpenLabel = getCmsText('layout.header', 'mobileMenuOpenLabel', 'Abrir menú de navegación');

function isActive(href: string) {
  if (href === '/') return currentPath === '/';
  return currentPath.startsWith(href);
}
---

<!--
  overlay=false (páginas internas): sticky, fondo blanco, bordes oscuros — idéntico al anterior.
  overlay=true (home): fixed transparente sobre el hero; al scroll >80px → clase is-scrolled → fondo blanco sólido.
-->
<header
  class={[
    'top-0 z-50 transition-[background-color,border-color,box-shadow,color] duration-[240ms] ease-standard',
    overlay
      ? 'fixed inset-x-0 bg-transparent border-b border-transparent'
      : 'sticky bg-white border-b border-border shadow-sm',
  ].join(' ')}
  data-overlay={overlay ? 'true' : undefined}
>
  <div class="w-full mx-auto px-6 max-w-container">
    <div class="flex items-center justify-between h-24">

      <!-- Logo -->
      <a href="/" class="flex items-center gap-4 shrink-0 no-underline" aria-label={getCmsText('layout.header', 'logoAriaLabel', 'Hidromont Chile — Inicio')}>
        {cmsLogoSrc ? (
          <EditableImage
            entry="layout.header"
            field="logoSrc"
            altField="logoAlt"
            src={cmsLogoSrc}
            alt={cmsLogoAlt}
            width={300}
            height={100}
            class={[
              'h-20 w-auto object-contain transition-[filter] duration-[240ms] ease-standard',
              overlay ? 'logo-img' : '',
            ].join(' ')}
            loading="eager"
          />
        ) : (
          <Image
            src={logo}
            alt={cmsLogoAlt}
            width={300}
            height={100}
            class={[
              'h-20 w-auto object-contain transition-[filter] duration-[240ms] ease-standard',
              overlay ? 'logo-img' : '',
            ].join(' ')}
            loading="eager"
            {...(import.meta.env.DEV ? {
              'data-cms-entry': 'layout.header',
              'data-cms-field': 'logoSrc',
              'data-cms-alt-field': 'logoAlt',
              'data-cms-type': 'image',
            } : {})}
          />
        )}
        <div class="flex flex-col leading-tight">
          <span
            class={[
              'font-heading font-bold tracking-wide transition-colors duration-[240ms] ease-standard',
              overlay ? 'text-white wordmark-primary' : 'text-primary-dark',
            ].join(' ')}
            style="font-size: 1.45rem; line-height: 1.15;"
          >
            <EditableText entry="layout.header" field="wordmarkPrimary">
              {getCmsText('layout.header', 'wordmarkPrimary', 'HIDROMONT')}
            </EditableText>
          </span>
          <span
            class={[
              'font-body tracking-widest uppercase transition-colors duration-[240ms] ease-standard',
              overlay ? 'text-white/80 wordmark-sub' : 'text-text-muted',
            ].join(' ')}
            style="font-size: 0.75rem; letter-spacing: 0.18em;"
          >
            <EditableText entry="layout.header" field="wordmarkSub">
              {getCmsText('layout.header', 'wordmarkSub', 'Chile S.A.')}
            </EditableText>
          </span>
        </div>
      </a>

      <!-- Desktop nav -->
      <nav aria-label="Navegación principal" class="hidden lg:flex items-center gap-1">
        {navItems.map((item) => (
          item.children ? (
            <div class="relative group">
              <a
                href={item.href}
                class={[
                  'px-4 py-2 text-sm font-semibold font-body rounded-md transition-colors duration-fast ease-standard no-underline',
                  overlay
                    ? `nav-link hover:bg-white/10 hover:text-white ${isActive(item.href) ? 'text-white' : 'text-white/90'}`
                    : `hover:bg-primary-light hover:text-primary-dark ${isActive(item.href) ? 'text-primary' : 'text-text'}`,
                ].join(' ')}
              >
                <EditableText entry="layout.header" field={item.cmsField}>{item.label}</EditableText>
                <svg class="inline-block ml-1 w-3 h-3" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M2 4l4 4 4-4" />
                </svg>
              </a>
              <div class="absolute top-full left-0 mt-1 w-56 bg-white border border-border rounded-lg shadow-md opacity-0 invisible group-hover:opacity-100 group-hover:visible group-focus-within:opacity-100 group-focus-within:visible transition-[opacity,visibility] duration-base ease-standard z-10">
                {item.children.map((child) => (
                  <a
                    href={child.href}
                    class={`block px-4 py-2.5 text-sm font-body no-underline transition-colors duration-fast hover:bg-primary-light hover:text-primary-dark first:rounded-t-lg last:rounded-b-lg ${isActive(child.href) ? 'text-primary font-semibold' : 'text-text'}`}
                  >
                    <EditableText entry="layout.header" field={child.cmsField}>{child.label}</EditableText>
                  </a>
                ))}
              </div>
            </div>
          ) : (
            <a
              href={item.href}
              class={[
                'px-4 py-2 text-sm font-semibold font-body rounded-md transition-colors duration-fast ease-standard no-underline',
                overlay
                  ? `nav-link hover:bg-white/10 hover:text-white ${isActive(item.href) ? 'text-white' : 'text-white/90'}`
                  : `hover:bg-primary-light hover:text-primary-dark ${isActive(item.href) ? 'text-primary' : 'text-text'}`,
              ].join(' ')}
            >
              <EditableText entry="layout.header" field={item.cmsField}>{item.label}</EditableText>
            </a>
          )
        ))}
      </nav>

      <!-- Desktop CTA -->
      <div class="hidden lg:flex items-center gap-3">
        <Button href={ctaHref} size="md">
          <EditableText entry="layout.header" field="ctaLabel">{ctaLabel}</EditableText>
        </Button>
      </div>

      <!-- Mobile menu toggle -->
      <button
        id="menu-toggle"
        type="button"
        class={[
          'lg:hidden flex items-center justify-center w-11 h-11 rounded-md transition-colors duration-fast',
          overlay ? 'text-white hover:bg-white/10 toggle-btn' : 'text-text hover:bg-primary-light',
        ].join(' ')}
        aria-expanded="false"
        aria-controls="mobile-menu"
        aria-label={mobileMenuOpenLabel}
      >
        <svg id="icon-menu" class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <path d="M4 6h16M4 12h16M4 18h16" />
        </svg>
        <svg id="icon-close" class="w-6 h-6 hidden" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <path d="M6 6l12 12M6 18L18 6" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile menu (siempre fondo blanco cuando se abre) -->
  <div id="mobile-menu" class="lg:hidden hidden border-t border-border bg-white">
    <nav aria-label="Navegación móvil" class="px-4 py-4 space-y-1">
      {navItems.map((item) => (
        item.children ? (
          <details class="group">
            <summary class={`flex items-center justify-between w-full px-3 py-2.5 text-sm font-semibold font-body rounded-md cursor-pointer list-none transition-colors hover:bg-primary-light ${isActive(item.href) ? 'text-primary' : 'text-text'}`}>
              <EditableText entry="layout.header" field={item.cmsField}>{item.label}</EditableText>
              <svg class="w-4 h-4 transition-transform group-open:rotate-180" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M2 4l4 4 4-4" />
              </svg>
            </summary>
            <div class="mt-1 ml-3 space-y-1 border-l-2 border-primary-light pl-3">
              {item.children.map((child) => (
                <a
                  href={child.href}
                  class={`block px-3 py-2 text-sm font-body no-underline rounded-md transition-colors hover:bg-primary-light hover:text-primary-dark ${isActive(child.href) ? 'text-primary font-semibold' : 'text-text-muted'}`}
                >
                  <EditableText entry="layout.header" field={child.cmsField}>{child.label}</EditableText>
                </a>
              ))}
            </div>
          </details>
        ) : (
          <a
            href={item.href}
            class={`block px-3 py-2.5 text-sm font-semibold font-body rounded-md no-underline transition-colors hover:bg-primary-light hover:text-primary-dark ${isActive(item.href) ? 'text-primary bg-primary-light' : 'text-text'}`}
          >
            <EditableText entry="layout.header" field={item.cmsField}>{item.label}</EditableText>
          </a>
        )
      ))}
      <div class="pt-4 border-t border-border">
        <Button href={ctaHref} class="w-full justify-center">
          <EditableText entry="layout.header" field="ctaLabel">{ctaLabel}</EditableText>
        </Button>
      </div>
    </nav>
  </div>
</header>

<script>
  /* ── Mobile menu toggle ───────────────────────────────────── */
  const toggle = document.getElementById('menu-toggle');
  const menu = document.getElementById('mobile-menu');
  const iconMenu = document.getElementById('icon-menu');
  const iconClose = document.getElementById('icon-close');

  toggle?.addEventListener('click', () => {
    const isOpen = !menu?.classList.contains('hidden');
    menu?.classList.toggle('hidden', isOpen);
    const opening = !isOpen;
    toggle.setAttribute('aria-expanded', String(opening));
    toggle.setAttribute('aria-label', opening ? 'Cerrar menú de navegación' : 'Abrir menú de navegación');
    iconMenu?.classList.toggle('hidden', opening);
    iconClose?.classList.toggle('hidden', !opening);
  });

  /* ── Overlay scroll behaviour (home only) ────────────────── */
  const headerEl = document.querySelector<HTMLElement>('header[data-overlay]');
  if (headerEl) {
    const header: HTMLElement = headerEl;
    const THRESHOLD = 80;
    let ticking = false;
    let scrolled = false;

    // Elements that change colour on scroll
    const navLinks = header.querySelectorAll<HTMLElement>('.nav-link');
    const wordmarkPrimary = header.querySelector<HTMLElement>('.wordmark-primary');
    const wordmarkSub = header.querySelector<HTMLElement>('.wordmark-sub');
    const logoImg = header.querySelector<HTMLImageElement>('.logo-img');
    const toggleBtn = header.querySelector<HTMLElement>('.toggle-btn');

    function applyScrolled(yes: boolean) {
      if (yes === scrolled) return;
      scrolled = yes;

      if (yes) {
        // Solid white state
        header.classList.add('bg-white', 'border-border', 'shadow-sm');
        header.classList.remove('bg-transparent', 'border-transparent');
        navLinks.forEach((el) => {
          el.classList.remove('text-white/90', 'text-white', 'hover:bg-white/10', 'hover:text-white');
          el.classList.add('text-text', 'hover:bg-primary-light', 'hover:text-primary-dark');
        });
        if (wordmarkPrimary) {
          wordmarkPrimary.classList.remove('text-white');
          wordmarkPrimary.classList.add('text-primary-dark');
        }
        if (wordmarkSub) {
          wordmarkSub.classList.remove('text-white/80');
          wordmarkSub.classList.add('text-text-muted');
        }
        if (logoImg) logoImg.style.filter = '';
        if (toggleBtn) {
          toggleBtn.classList.remove('text-white', 'hover:bg-white/10');
          toggleBtn.classList.add('text-text', 'hover:bg-primary-light');
        }
      } else {
        // Transparent state
        header.classList.remove('bg-white', 'border-border', 'shadow-sm');
        header.classList.add('bg-transparent', 'border-transparent');
        navLinks.forEach((el) => {
          el.classList.add('text-white/90', 'hover:bg-white/10', 'hover:text-white');
          el.classList.remove('text-text', 'text-primary', 'hover:bg-primary-light', 'hover:text-primary-dark');
        });
        if (wordmarkPrimary) {
          wordmarkPrimary.classList.add('text-white');
          wordmarkPrimary.classList.remove('text-primary-dark');
        }
        if (wordmarkSub) {
          wordmarkSub.classList.add('text-white/80');
          wordmarkSub.classList.remove('text-text-muted');
        }
        if (logoImg) logoImg.style.filter = 'brightness(0) invert(1)';
        if (toggleBtn) {
          toggleBtn.classList.add('text-white', 'hover:bg-white/10');
          toggleBtn.classList.remove('text-text', 'text-primary', 'hover:bg-primary-light', 'hover:text-primary-dark');
        }
      }
    }

    // Apply initial invert on load (transparent state)
    if (logoImg) logoImg.style.filter = 'brightness(0) invert(1)';

    window.addEventListener('scroll', () => {
      if (!ticking) {
        requestAnimationFrame(() => {
          applyScrolled(window.scrollY > THRESHOLD);
          ticking = false;
        });
        ticking = true;
      }
    }, { passive: true });
  }
</script>
